import React from 'react';

const Loading = () => {
    return (
        <div className='flex justify-center items-center h-96'>
            <button className='btn loading mx-auto text-white'>Loading</button>
        </div>
    );
};

export default Loading;